.venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
coverage.xml
*.egg-info/
GG/
node_modules/
*.db
*.sqlite
*.sqlite3
!**/schema.sql
workspace/**/data/*.db
workspace/**/logs/*.log
.cache/

# Backups / snapshots (never commit)
*.bak
**/.snapshots/*.patch.bak

# CCG runtime task state (mutates every turn — track via .context/ instead)
.ccg/tasks/

# uv project lock is tracked explicitly (committed below for repro)

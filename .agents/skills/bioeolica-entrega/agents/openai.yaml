interface:
  display_name: "Bioeolica Entrega"
  short_description: "Repo-specific patterns and workflows for bioeolica-entrega"
  default_prompt: "Use the bioeolica-entrega repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true